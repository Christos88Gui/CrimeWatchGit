@{
    ViewBag.Title = "Upload";
    Layout = "~/Views/Shared/_LayoutPortal.cshtml";
}
<br /><br /><br />
            <button style="float:left" class="btn btn-warning" onclick="location.href='@Url.Action("MyPortal", "Home")'"> User Portal</button>
<div class="container" style="width:350px; margin-top:4%;">
    <h3 class="title">Select Excel File</h3>
    <br />
    <div class="container customCont">
        <br /><br />
        @using (Html.BeginForm("ImportCrimes", "Import", FormMethod.Post, new { enctype = "multipart/form-data" }))
        {
            <input type="file" name="excelfile" />
            <br />
            <button type="submit" class="btn btn-primary"> Upload Crimes</button>
        }
        <br />
         @using (Html.BeginForm("ImportPoliceDepartments", "Import", FormMethod.Post, new { enctype = "multipart/form-data" }))
        {
            <input type="file" name="excelfile" />
            <br />
            <button type="submit" class="btn btn-primary"> Upload PD</button>
        } 
        <br />
         @using (Html.BeginForm("FindCounties", "Import", FormMethod.Post, new { enctype = "multipart/form-data" }))
        {
            <input type="file" name="excelfile" />
            <br />
            <button type="submit" class="btn btn-primary"> FindCounties</button>
        }   
    
    </div>
</div>


<style>
    input {
        height: 40px;
        font-size: 16px;
        color: black;
        font-weight: bold;
    }

    body {
        background: #f4f4f4;
    }
</style>
